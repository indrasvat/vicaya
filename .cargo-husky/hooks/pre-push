#!/bin/sh
# Pre-push hook for vicaya
# Runs format, lint, and tests before allowing push

set -e

echo "Running pre-push checks..."

echo "→ Formatting code..."
make fmt

echo "→ Running linter..."
make lint

echo "→ Running tests..."
make test

echo "✅ All pre-push checks passed!"
